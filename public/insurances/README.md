# Insurance logo assets

Drop insurance company logo images here (e.g. PNG, SVG).

- Files are served at `/insurances/filename.ext`
- Reference them in `src/data/insurances.ts` as `image: "/insurances/yourfile.png"`
- Replace or remove `placeholder.svg` when you add real logos
